<%= simple_form_for @course do |f| %>
  <%= f.input :title, label: "Course Title"  %>
  <%= f.input :description, label: "Review" %>
  <%= f.input :host, label: "Who is the course run by?" %>
  <%= f.button :submit %>
<% end %>
